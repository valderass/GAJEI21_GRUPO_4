using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ttemporal : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("repositorio");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
